# SwiftPackageManagerSharedHelpers
Testing Repo For Sharing Helpers Across Packages

## Goal
The goal is to eventually share the `byPlatform` and `onlyApple` functions across Package.swift files so that we can share the logic for splitting data on build platforms.
